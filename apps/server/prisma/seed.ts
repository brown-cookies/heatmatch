// Universities are now hardcoded on the client — nothing to seed.
// This file is kept as a placeholder in case future seed data is needed.

async function main() {
  console.log("Nothing to seed.");
}

main().catch((e) => { console.error(e); process.exit(1); });
